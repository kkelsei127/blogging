<!-- Loop over each object in the posts array -->
{{#each posts }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <a href="/post/{{id}}">{{post_name}}</a>
    </h2>
    {{!-- <p>
      {{{post_body}}}
    </p> --}}
    <p>Created on {{format_date createdAt}}</p>
  </div>
  {{!-- <div class="col-md-7">
    <p>
        <!--this might be wrong on how to target the comments -->
      {{{comment_id}}}
    </p>
  </div> --}}
</div>
{{/each}}
